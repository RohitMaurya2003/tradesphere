services:
  - type: web
    name: tradesphere-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install --production
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
